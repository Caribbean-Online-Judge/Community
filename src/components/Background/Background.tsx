import { BackgroundContainer, BackgroundStyled, Blur } from "./styled";

function Background() {
  return (
    <BackgroundContainer>
      <BackgroundStyled />
      <Blur />
    </BackgroundContainer>
  );
}

export default Background;
